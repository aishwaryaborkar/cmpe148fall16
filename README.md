Android application with a basic Linear Layout that allows a user to add items to a list. 
Items to the list are saved to the backend (Firebase). 

Next development goal: add user authentication through email. 
